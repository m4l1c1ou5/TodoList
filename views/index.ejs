<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>TodoList</title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bulma@0.9.2/css/bulma.min.css">
</head>
<body>
    <div class="columns">
        <div class="column">
            <div class="card">
                <div class="card-content">
                    <p class="title is-4">Your TodoList</p>
                    <div class="content">
                        <ol>
                        <% for(i=0;i<arr.length;i++){ %>
                            <% if(i%2){ %>
                            <li><p style="color:cyan"><%= arr[i] %></p></li>
                            <% }else{ %>
                            <li><p style="color:darkorange"><%= arr[i] %></p></li>
                        <% }} %>
                        </ol>
                    </div>
                    <p class="title is-4">Done an Item??</p>
                    <form method="POST" action="/">
                        <input class="input is-danger" type="number" name="item_no" placeholder="Item No.">
                        <button class="button is-warning" type="submit" name="sub" value="rem">Remove the Item</button>
                    </form>
                </div>
            </div>
        </div>
        <div class="column">
            <div class="card">
            <div class="card-content">
            <p class="title is-4">Add more Item to Todolist!</p>
            <form method="POST" action="/">
                <input class="input is-primary is-focused" type="text" name="item" placeholder="Item Name">
                <button class="button is-warning" type="submit"name="sub" value="add">Add to my list</button>
            </form>
            </div>
            </div>
        </div>
    </div>    
</body>
</html>